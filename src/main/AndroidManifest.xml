<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.carmelmusicacademyapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".TestApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SubRequestList"></activity>
        <activity android:name=".SubRequestForm" />
        <activity android:name=".DeleteMusicalLadderStudent" />
        <activity android:name=".BandProspectsList" />
        <activity android:name=".BandProspects" />
        <activity android:name=".TeacherContacts" />
        <activity android:name=".MusicalLadder" />
        <activity android:name=".NavigationMenu" />
        <activity android:name=".ActivateAccount" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>